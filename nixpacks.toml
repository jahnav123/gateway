[phases.setup]
nixPkgs = ["python39", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python init_db_postgres.py"]

[start]
cmd = "uvicorn server:app --host 0.0.0.0 --port $PORT"
