[phases.setup]
nixPkgs = ["python39", "python39Packages.pip", "postgresql"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = ["python3 init_db_postgres.py"]

[start]
cmd = "python3 -m uvicorn server:app --host 0.0.0.0 --port $PORT"
